# Pinned to the exact versions currently baked into the tested image so a
# `--build` can't silently pull a breaking major of instructor/openai/etc. into
# the production-like image. Bump deliberately and re-test. (M25)

# FastAPI and server
fastapi==0.139.0
uvicorn[standard]==0.49.0

# Database
sqlmodel==0.0.39
alembic==1.18.5
psycopg2-binary==2.9.12  # PostgreSQL driver (production DATABASE_URL)

# AI Integration
openai==2.44.0
instructor==1.15.4
anthropic==0.115.1

# Utilities
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
